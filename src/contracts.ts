export interface Options {
    rootDir: string;
    outDir: string;
    removeSource?: boolean;
    pattern?: string;
    watch?: boolean;
    suffix?: string;
    prefix?: string;
    header?: string;
}
